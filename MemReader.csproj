<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <PlatformTarget>x64</PlatformTarget>
    <Platforms>x64</Platforms>
    <AssemblyName>MemReader</AssemblyName>
    <RootNamespace>MemReader</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <!-- Fuente moderna de Windows 11 (la consume ApplicationConfiguration.Initialize()).
         Se mantiene 9pt: el layout usa posiciones absolutas calibradas para ese tamano. -->
    <ApplicationDefaultFont>Segoe UI, 9pt</ApplicationDefaultFont>
    <Product>MemReader</Product>
    <AssemblyTitle>MemReader - Lector de memoria de procesos (user-mode)</AssemblyTitle>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <!-- Desensamblador x86/x64 (gestionado, compatible con publish single-file). -->
    <PackageReference Include="Iced" Version="1.21.0" />
  </ItemGroup>

</Project>
